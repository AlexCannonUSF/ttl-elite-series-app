# Stream-CV Module Placeholder

This top-level folder reserves the future standalone Stream-CV workspace/module for TTLElite Series 3.0.

## Current Phase 00 layout

- The compile-time scaffold lives in `/Users/alexcannon/Downloads/TTLEliteSeries/src/main/java/com/ttl/tabletennis/cv`.
- The runtime flag source is `/Users/alexcannon/Downloads/TTLEliteSeries/features.yaml`.
- No workers, OCR, or stream ingestion run yet while `features.stream-cv=off`.

We keep the scaffold inside the backend app for now so the code is testable without converting the repo to a multi-module build before the rest of Phase 00 is finished.
