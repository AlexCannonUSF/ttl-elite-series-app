# Frontend Lib Package

This folder contains browser-side helpers.

## Files

- `api.ts`
  - frontend API boundary and typed request helpers
- `format.ts`
  - formatting helpers used across pages

If the frontend needs a new backend route, start in `api.ts`.
