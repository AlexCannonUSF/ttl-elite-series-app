# Frontend Components Package

This folder contains shared UI shell pieces.

## Files

- `AppShell.tsx`
  - top-level navigation and page chrome

If more shared layout primitives are added later, they should live here instead of inside page files.
