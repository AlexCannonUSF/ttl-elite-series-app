# Projection Package

This package contains repository projection types used for aggregate queries.

## Files

- `HeadToHeadAggregateProjection`
- `PlayerStatsAggregateProjection`

If you need a new aggregate query result shape and it is repository-specific, it belongs here.
